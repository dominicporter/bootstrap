name: Bootstrap
on:
  push:
    branches:
    - master
jobs:
  dep:
    runs-on: windows-2019
    steps:
    - uses: actions/checkout@master
    - uses: ./
    - run: git checkout master
    - name: Run a one-line script
      run: .\win\bootstrap.ps1
      shell: powershell